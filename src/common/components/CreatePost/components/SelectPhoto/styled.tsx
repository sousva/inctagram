import styled from 'styled-components'

export const SelectPhotoWrapper = styled.div`
    display: flex;
    flex-direction: column;
    margin: 0 auto;
    gap: 20px;
    max-width: 230px;
    z-index: 1;
    position: absolute;
    bottom: 0;
`
