import React from 'react'

export const metadata = {
    title: 'Statistics',
    description: 'Statistics on your account',
}

export default function StatisticsLayout({children}: {children: React.ReactNode}) {
    return <>{children}</>
}
