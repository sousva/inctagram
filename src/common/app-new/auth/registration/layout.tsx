import React from 'react'

export const metadata = {
    title: 'Registration page',
    description: 'Here you can register a new account',
}

export default function RegistrationLayout({children}: {children: React.ReactNode}) {
    return <>{children}</>
}
