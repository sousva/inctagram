import React from 'react'

export const metadata = {
    title: 'Login page',
    description: 'Login page, here you can access in your account',
}

export default function RegistrationLayout({children}: {children: React.ReactNode}) {
    return <>{children}</>
}
